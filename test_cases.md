# 测试用例

## 测试用例1：产品需求 → 开发视角

### 输入内容（产品视角）：
```
我们需要在APP首页增加一个个性化推荐模块，根据用户的浏览历史和购买记录，推荐他们可能感兴趣的商品。要求推荐结果实时更新，用户每次刷新都能看到新的推荐内容。希望这个功能能在2周内上线。
```

### 预期输出要点（开发视角）：
- 推荐算法类型建议（如协同过滤、内容推荐等）
- 数据来源和处理方式（用户行为数据、商品数据等）
- 性能和实时性要求（响应时间、并发处理等）
- 预估开发工作量（技术难度、时间评估等）

---

## 测试用例2：技术方案 → 产品视角

### 输入内容（开发视角）：
```
我们计划使用Redis缓存来优化商品列表页的加载速度。通过将热点商品数据缓存到Redis中，可以将数据库查询时间从平均200ms降低到10ms以内。同时使用CDN加速静态资源加载，预计整体页面加载时间可以减少60%。这个方案需要增加Redis服务器和CDN服务的成本，但可以显著提升用户体验。
```

### 预期输出要点（产品视角）：
- 对用户体验的实际影响（页面加载速度提升、操作更流畅等）
- 支持的业务增长空间（支持更多用户访问、提升转化率等）
- 成本降低的商业价值（减少服务器压力、提升用户满意度等）

---

## 使用说明

1. 启动服务后，访问 http://localhost:8000
2. 选择翻译方向（产品→开发 或 开发→产品）
3. 将上述测试用例的输入内容粘贴到输入框
4. 点击"开始翻译"按钮
5. 观察流式输出的翻译结果
6. 验证输出是否包含预期的要点

